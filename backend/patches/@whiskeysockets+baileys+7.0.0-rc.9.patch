diff --git a/node_modules/@whiskeysockets/baileys/lib/Socket/messages-recv.js b/node_modules/@whiskeysockets/baileys/lib/Socket/messages-recv.js
index 15dd0e8..117b696 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Socket/messages-recv.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Socket/messages-recv.js
@@ -1866,7 +1866,11 @@ export const makeMessagesRecvSocket = (config) => {
                 // may create mappings via storeLIDPNMappings() without calling migrateSession()
                 // This leaves sessions under PN format while decrypt() expects LID format
                 // Skipping migration based on mapping existence causes "No session record" errors
-                await signalRepository.migrateSession(primaryJid, alt);
+                await signalRepository.migrateSession(primaryJid, alt).catch(error => logger.warn({
+                    error,
+                    primaryJid,
+                    alt
+                }, 'LID session migration failed, continuing with current message'));
             }
             else {
                 // Check if reverse mapping exists
@@ -1880,7 +1884,11 @@ export const makeMessagesRecvSocket = (config) => {
                 }
                 // CRITICAL: ALWAYS migrate session, even if mapping exists
                 // Same reasoning as above - mapping existence doesn't guarantee session migration
-                await signalRepository.migrateSession(alt, primaryJid);
+                await signalRepository.migrateSession(alt, primaryJid).catch(error => logger.warn({
+                    error,
+                    primaryJid,
+                    alt
+                }, 'PN session migration failed, continuing with current message'));
             }
         }
         if (msg.key?.remoteJid && msg.key?.id && messageRetryManager) {
@@ -2012,6 +2020,74 @@ export const makeMessagesRecvSocket = (config) => {
                         }
                         return sendMessageAck(node);
                     }
+                    const normalizedFromJid = node.attrs.from?.replace(/:\d+@/, '@');
+                    const isDirectFromMeCompanionSync = !isJidGroup(msg.key.remoteJid) &&
+                        !msg.key.participant &&
+                        !!(node.attrs.recipient || node.attrs.peer_recipient_pn || node.attrs.peer_recipient_lid) &&
+                        (msg.key.fromMe ||
+                            areJidsSameUser(node.attrs.from, authState.creds.me.id) ||
+                            areJidsSameUser(node.attrs.from, authState.creds.me.lid || '') ||
+                            areJidsSameUser(normalizedFromJid, authState.creds.me.id) ||
+                            areJidsSameUser(normalizedFromJid, authState.creds.me.lid || ''));
+                    if (isDirectFromMeCompanionSync && msg.key) {
+                        const startTime = Date.now();
+                        const msgId = msg.key.id;
+                        const msgKey = msg.key;
+                        const msgData = {
+                            key: { ...msgKey },
+                            pushName: msg.pushName,
+                            messageTimestamp: msg.messageTimestamp,
+                            participant: msg.key.participant,
+                            participantAlt: msg.key.participantAlt
+                        };
+                        logger.info({
+                            msgId,
+                            remoteJid: msgKey.remoteJid,
+                            author: node.attrs.from,
+                            recipient: node.attrs.recipient,
+                            peerRecipientPn: node.attrs.peer_recipient_pn
+                        }, 'Direct fromMe companion sync decryption failed, requesting placeholder resend');
+                        if (messageRetryManager) {
+                            metrics.ctwaRecoveryRequests.inc({ status: 'scheduled' });
+                            messageRetryManager.schedulePhoneRequest(msgId, async () => {
+                                try {
+                                    const requestId = await requestPlaceholderResend(msgKey, msgData);
+                                    if (requestId && requestId !== 'RESOLVED') {
+                                        logger.debug({ msgId, requestId }, 'Direct fromMe companion sync: placeholder resend request sent successfully');
+                                        metrics.ctwaRecoveryRequests.inc({ status: 'sent' });
+                                    }
+                                    else if (requestId === 'RESOLVED') {
+                                        logger.debug({ msgId }, 'Direct fromMe companion sync: message recovered during resend delay');
+                                        metrics.ctwaMessagesRecovered.inc();
+                                        metrics.ctwaRecoveryLatency.observe(Date.now() - startTime);
+                                    }
+                                }
+                                catch (error) {
+                                    logger.warn({ error, msgId }, 'Direct fromMe companion sync: failed to request placeholder resend');
+                                    metrics.ctwaRecoveryFailures.inc({ reason: 'request_failed' });
+                                }
+                            });
+                        }
+                        else {
+                            metrics.ctwaRecoveryRequests.inc({ status: 'requested' });
+                            try {
+                                const requestId = await requestPlaceholderResend(msgKey, msgData);
+                                if (requestId && requestId !== 'RESOLVED') {
+                                    logger.debug({ msgId, requestId }, 'Direct fromMe companion sync: placeholder resend request sent successfully (direct)');
+                                }
+                                else if (requestId === 'RESOLVED') {
+                                    logger.debug({ msgId }, 'Direct fromMe companion sync: message recovered before direct resend request completed');
+                                    metrics.ctwaMessagesRecovered.inc();
+                                    metrics.ctwaRecoveryLatency.observe(Date.now() - startTime);
+                                }
+                            }
+                            catch (error) {
+                                logger.warn({ error, msgId }, 'Direct fromMe companion sync: failed to request placeholder resend');
+                                metrics.ctwaRecoveryFailures.inc({ reason: 'request_failed' });
+                            }
+                        }
+                        return sendMessageAck(node);
+                    }
                     // Skip retry for expired status messages (>24h old)
                     if (isJidStatusBroadcast(msg.key.remoteJid)) {
                         const messageAge = unixTimestampSeconds() - toNumber(msg.messageTimestamp);
diff --git a/node_modules/@whiskeysockets/baileys/lib/Utils/decode-wa-message.js b/node_modules/@whiskeysockets/baileys/lib/Utils/decode-wa-message.js
index d1c7d1a..de96177 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Utils/decode-wa-message.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Utils/decode-wa-message.js
@@ -18,7 +18,11 @@ const storeMappingFromEnvelope = async (stanza, sender, repository, decryptionJi
     if (senderAlt && isLidUser(senderAlt) && isPnUser(sender) && decryptionJid === sender) {
         try {
             await repository.lidMapping.storeLIDPNMappings([{ lid: senderAlt, pn: sender }]);
-            await repository.migrateSession(sender, senderAlt);
+            await repository.migrateSession(sender, senderAlt).catch(error => logger.warn({
+                sender,
+                senderAlt,
+                error
+            }, 'Failed to migrate envelope session mapping'));
             logger.debug({ sender, senderAlt }, 'Stored LID mapping from envelope');
         }
         catch (error) {
@@ -126,11 +130,12 @@ export function decodeMessageNode(stanza, meId, meLid) {
     if (!from) {
         throw new Boom('Missing from attribute in message', { data: stanza });
     }
+    const normalizeCompanionJid = (jid) => jid?.replace(/:\d+@/, '@');
     const participant = stanza.attrs.participant;
     const recipient = stanza.attrs.recipient;
     const addressingContext = extractAddressingContext(stanza);
-    const isMe = (jid) => areJidsSameUser(jid, meId);
-    const isMeLid = (jid) => areJidsSameUser(jid, meLid);
+    const isMe = (jid) => areJidsSameUser(jid, meId) || areJidsSameUser(normalizeCompanionJid(jid), meId);
+    const isMeLid = (jid) => areJidsSameUser(jid, meLid) || areJidsSameUser(normalizeCompanionJid(jid), meLid);
     if (isPnUser(from) || isLidUser(from) || isHostedLidUser(from) || isHostedPnUser(from)) {
         if (recipient && !isJidMetaAI(recipient)) {
             if (!isMe(from) && !isMeLid(from)) {
@@ -185,9 +190,12 @@ export function decodeMessageNode(stanza, meId, meLid) {
         throw new Boom('Unknown message type', { data: stanza });
     }
     const pushname = stanza?.attrs?.notify;
+    const directChatAltJid = !isJidGroup(chatId)
+        ? (fromMe ? (addressingContext.recipientAlt || addressingContext.senderAlt) : addressingContext.senderAlt)
+        : undefined;
     const key = {
         remoteJid: chatId,
-        remoteJidAlt: !isJidGroup(chatId) ? addressingContext.senderAlt : undefined,
+        remoteJidAlt: directChatAltJid,
         fromMe,
         id: msgId,
         participant,
@@ -242,7 +250,16 @@ export const decryptMessageNode = (stanza, meId, meLid, repository, logger) => {
                     }
                     decryptables += 1;
                     let msgBuffer;
-                    const decryptionJid = await getDecryptionJid(author, repository);
+                    const isOwnDirectCompanionMessage = !isJidGroup(fullMessage.key.remoteJid) &&
+                        !!stanza.attrs.recipient &&
+                        (areJidsSameUser(author, meId) ||
+                            areJidsSameUser(author, meLid || '') ||
+                            areJidsSameUser(author?.replace(/:\d+@/, '@'), meId) ||
+                            areJidsSameUser(author?.replace(/:\d+@/, '@'), meLid || ''));
+                    const directChatDecryptionTarget = (fullMessage.key.fromMe || isOwnDirectCompanionMessage) && !isJidGroup(fullMessage.key.remoteJid)
+                        ? (fullMessage.key.remoteJidAlt || fullMessage.key.remoteJid || author)
+                        : author;
+                    const decryptionJid = await getDecryptionJid(directChatDecryptionTarget, repository);
                     if (tag !== 'plaintext') {
                         // TODO: Handle hosted devices
                         await storeMappingFromEnvelope(stanza, author, repository, decryptionJid, logger);
diff --git a/node_modules/@whiskeysockets/baileys/lib/Utils/process-message.js b/node_modules/@whiskeysockets/baileys/lib/Utils/process-message.js
index 21782ed..781a906 100644
--- a/node_modules/@whiskeysockets/baileys/lib/Utils/process-message.js
+++ b/node_modules/@whiskeysockets/baileys/lib/Utils/process-message.js
@@ -373,6 +373,17 @@ const processMessage = async (message, { shouldProcessHistoryMsg, placeholderRes
                             // This ensures we don't lose critical information like pushName and LID mappings
                             // eslint-disable-next-line max-depth
                             if (cachedData && typeof cachedData === 'object') {
+                                if (cachedData.key && webMessageInfo.key) {
+                                    const cachedKey = cachedData.key;
+                                    const recoveredKey = webMessageInfo.key;
+                                    if (!recoveredKey.remoteJidAlt && cachedKey.remoteJidAlt) {
+                                        recoveredKey.remoteJidAlt = cachedKey.remoteJidAlt;
+                                        logger?.debug({ msgId: webMessageInfo.key?.id, remoteJidAlt: cachedKey.remoteJidAlt }, 'CTWA: Restored remoteJidAlt from cached metadata');
+                                    }
+                                    if (!recoveredKey.addressingMode && cachedKey.addressingMode) {
+                                        recoveredKey.addressingMode = cachedKey.addressingMode;
+                                    }
+                                }
                                 // Preserve pushName if not present in PDO response
                                 // eslint-disable-next-line max-depth
                                 if (cachedData.pushName && !webMessageInfo.pushName) {
